name: flats_mlmodel

conda_env: conda.yaml

entry_points:

  download_latest_data:
    command: "python download_latest_data.py"
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python train.py {alpha} {l1_ratio}"
